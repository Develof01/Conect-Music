package com.example.conct_music.utils

class Const {

    companion object {
        const val DB_NAME = "conectmusic.db"
        const val DB_VERSION = 1
    }

    class RegularExpresion {
        companion object {
            const val PWD_RE = "((?=.*\\d)(?=.*[@#\$%*]).{4,8})"
        }
    }

}